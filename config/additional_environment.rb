# Additional environment configuration for Glimfo Projects

# Force logger to STDOUT for Docker
config.logger = Logger.new(STDOUT)
config.logger.level = Logger::INFO

# Add any Glimfo-specific initializers or environment overrides here
